import WallCalendar from "@/components/WallCalendar";

/**
 * Home page — renders the wall calendar component.
 * This is the single page of the application.
 */
export default function Home() {
  return (
    <main>
      <WallCalendar />
    </main>
  );
}
